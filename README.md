# be-img-builder-cli
Orchestrate rpi-image-builder for easier CLI usage


## Clone repo with sub-module(s)

    git clone --recursive REPO_URL


## Init sub-module(s) if you cloned without 'recursive'


    git submodule init
    git submodule update --remote

## Updating sub-module(s)

    git submodule update --remote
